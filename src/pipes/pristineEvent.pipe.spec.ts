import { PristineEventPipe } from './pristineEvent.pipe';

describe('PristineEventPipe', () => {
  it('create an instance', () => {
    const pipe = new PristineEventPipe();
    expect(pipe).toBeTruthy();
  });
});
